#pragma once
#include"stdafx.h"
#include<list>
#include<string>
using namespace std;
static deque<string> expression;
static deque<string> result;

static int temp = 0;
static deque <Fraction> space;
static deque <Fraction> line; //Store the formula
